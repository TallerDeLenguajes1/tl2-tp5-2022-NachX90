@model CadeteriaMVC.ViewModels.Clientes.ListadoDeClientesVM
@{
    ViewData["Title"] = "Listado de Clientes";
}

            <h1 class="text-info">@ViewData["Title"]</h1>
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th scope="col">@Html.DisplayNameFor(Model => Model.ListaDeClientesVM.First().Nombre)</th>
                        <th scope="col">@Html.DisplayNameFor(Model => Model.ListaDeClientesVM.First().Domicilio)</th>
                        <th scope="col">@Html.DisplayNameFor(Model => Model.ListaDeClientesVM.First().Referencia)</th>
                        <th scope="col">@Html.DisplayNameFor(Model => Model.ListaDeClientesVM.First().Telefono)</th>
                        <th scope="col">Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var Cliente in Model.ListaDeClientesVM)
                    {
                    <tr>
                        <td scope="row" class="align-middle">@Cliente.Nombre</td>
                        <td class="align-middle">@Cliente.Domicilio</td>
                        <td class="align-middle">@Cliente.Referencia</td>
                        <td class="align-middle">@Cliente.Telefono</td>
                        <td class="align-middle">
                            <a class="btn btn-outline-info bg-gradient btn-sm my-1" asp-controller="Pedidos" asp-action="ListarPorCliente" asp-route-id="@Cliente.Id">
                                <svg class="bi" width="18" height="18" fill="currentColor">
                                  <use xlink:href="../../bootstrap-icons/bootstrap-icons.svg#list-ul"/>
                                </svg>
                                Listar
                            </a>
                            <a class="btn btn-outline-info bg-gradient btn-sm my-1" asp-controller="Clientes" asp-action="EditarCliente" asp-route-id="@Cliente.Id">
                                <svg class="bi" width="18" height="18" fill="currentColor">
                                  <use xlink:href="../../bootstrap-icons/bootstrap-icons.svg#pencil-fill"/>
                                </svg>
                                Editar
                            </a>
                            <a class="btn btn-outline-danger bg-gradient btn-sm my-1" asp-controller="Clientes" asp-action="EliminarCliente" asp-route-id="@Cliente.Id" onclick="    return confirm('¿Eliminar definitivamente? ¡Esta acción no se puede deshacer!');">
                                <svg class="bi" width="18" height="18" fill="currentColor">
                                  <use xlink:href="../../bootstrap-icons/bootstrap-icons.svg#trash-fill"/>
                                </svg>
                                Eliminar
                            </a>
                        </td>
                    </tr>
                    }
                </tbody>
            </table>
            