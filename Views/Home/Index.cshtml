@{
    ViewData["Title"] = "Inicio";
}

            <h1>Bienvenido</h1>
            <form asp-controller="Cadetes" asp-action="ListadoDeCadetes" method="post">
                <div class="form-group row m-2">
                    <label for="user" class="col-form-label col-sm-1">Usuario</label>
                    <div class="col-sm-2">
                        <input name="user" type="text" placeholder="Ingrese el usuario" class="form-control" required/>
                    </div>
                </div>
                <div class="form-group row m-2">
                    <label for="pass" class="col-form-label col-sm-1">Contraseña</label>
                    <div class="col-sm-2">
                        <input name="pass" type="password" placeholder="Ingrese la contraseña" class="form-control" required/>
                    </div>
                </div>
                <button type="submit" class="btn btn-primary col-sm-3 m-2">Ingresar</button>
            </form>
            <p>Este formulario no hace ningún control, solo redirecciona al listado de cadetes.</p>