@section Scripts { <partial name="_ValidationScriptsPartial" /> }
@model CadeteriaMVC.ViewModels.PedidoViewModel;
@{
    ViewData["Title"] = "Crear pedido";
}

            <h1>Crear pedido</h1>
            <form asp-controller="Pedidos" asp-action="CrearPedido" method="post">
@*                <div class="row m-2">
                    <label asp-for="@Model.ClienteViewModel" class="col-sm-2 col-form-label"></label>
                    <div class="col-sm-10">
                        <select asp-for="@Model.ClienteViewModel" class="form-select">
                            <option value="">[ID] Nombre</option>
@{
    foreach (var cliente in @Model.ListaDeClientesViewModel)
    {
                            <option value="@cliente">[@cliente.Id] @cliente.Nombre</option>
    }
}
                        </select>
                    </div>
                    <span asp-validation-for="@Model.ClienteViewModel" class="alert-warning"></span>
                </div>
*@                <div class="row m-2">
                    <label asp-for="@Model.Obs" class="col-sm-2 col-form-label"></label>
                    <div class="col-sm-10">
                        <input asp-for="@Model.Obs" placeholder="Ingrese las observaciones" class="form-control"/>
                    </div>
                    <span asp-validation-for="@Model.Obs" class="alert-warning"></span>
                </div>
                <div class="row m-2">
                    <input class="btn btn-primary m-2 col-sm-2" type="submit" value="Crear pedido">
                    <a asp-controller="Pedidos" asp-action="ListadoDePedidos" class="btn btn-primary m-2 col-sm-2">Cancelar</a>
                </div>
            </form>
